name: analog_clock_customizer
description: Analog clock with customizations.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # TODO: If you want to use your own clock package, add it here.
  analog_clock:
    path: ../analog_clock
  clock_customizer:
    path: ../clock_customizer
  model:
    path: ../model

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
