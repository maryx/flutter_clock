name: digital_clock
description: A digital clock package.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: "^0.16.0"
  model:
    path: ../model

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

    uses-material-design: true

    fonts:
     - family: PressStart2P
       fonts:
        - asset: lib/third_party/PressStart2P-Regular.ttf
